#include<bits/stdc++.h>
using namespace std;

#define ll long long

int main(){
    cin.tie(), cout.tie(), ios::sync_with_stdio(0);

    ll a, b;
    while(cin >> a >> b){
        cout << abs(a-b) << '\n';
    }
}